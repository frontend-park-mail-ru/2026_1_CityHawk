services:
  frontend:
    build: .
    ports:
      - "${HOST_PORT:-80}:${PORT:-8000}"
    environment:
      - PORT=${PORT:-8000}
      - API_BASE_URL=${API_BASE_URL:-http://localhost:8080}
    restart: unless-stopped