services:
  frontend:
    build: .
    ports:
      - "80:8000"
    environment:
      - PORT=8000
      - API_BASE_URL=http://cityhawk.ru:8080
    depends_on:
      - backend

  backend:
      image: 2026_1_cityhawk_backend-cityhawk-backend
      ports:
          - "8080:8080"
